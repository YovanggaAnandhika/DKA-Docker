```bash
# Generated by iptables-save v1.8.9 (nf_tables) on Fri Jan 24 19:53:30 2025
*filter
:INPUT ACCEPT [160:7968]
:FORWARD ACCEPT [1:302]
:OUTPUT ACCEPT [1139087:172034090]
:DOCKER - [0:0]
:DOCKER-ISOLATION-STAGE-1 - [0:0]
:DOCKER-ISOLATION-STAGE-2 - [0:0]
:DOCKER-USER - [0:0]

# ***************** LOCAL NETWORK ********************************************
# allow Loopback network
-A INPUT -i lo -j ACCEPT
#****************** GLOBAL TRAFFIC *******************************************
# Izinkan semua koneksi yang stabil atau related koneksi sebelumnya
-A INPUT -m conntrack --ctstate RELATED,ESTABLISHED -j ACCEPT
# Drop Koneksi Yang bersatus Invalid
-A INPUT -m conntrack --ctstate INVALID -j DROP
#******************* SSH TRAFFIC **********************************************
# Menset kedalam list address dan mengizinkan semua mask
-A INPUT -p tcp -m tcp --dport 22 -m recent --set --name DEFAULT --mask 255.255.255.255 --rsource
# Update Policy Bahwa Hanya Bisa Di hit 3 kali dalam 1 detik jika Lebih maka akan reject
-A INPUT -p tcp -m tcp --dport 22 -m recent --update --seconds 1 --hitcount 3 --name DEFAULT --mask 255.255.255.255 --rsource -j REJECT --reject-with tcp-reset
# Membuat Policy bahwa koneksi baru  harus dibawah 10 koneksi / 1 IP maximal 10 Koneksi jika lebih maka akan reject
-A INPUT -p tcp -m tcp --dport 22 -m conntrack --ctstate NEW -m connlimit --connlimit-above 10 --connlimit-mask 32 --connlimit-saddr -j REJECT --reject-with tcp-reset
# Jika memenu syarat maka koneksi di terima dan menuju status STABILIZE
-A INPUT -p tcp -m tcp --dport 22 -j ACCEPT
# ******************* FTP TRAFFIC ***********************************************
# Menset kedalam list address dan mengizinkan semua mask
-A INPUT -p tcp -m tcp --dport 21 -m recent --set --name DEFAULT --mask 255.255.255.255 --rsource
# Update Policy Bahwa Hanya Bisa Di hit 3 kali dalam 1 detik jika Lebih maka akan reject
-A INPUT -p tcp -m tcp --dport 21 -m recent --update --seconds 1 --hitcount 3 --name DEFAULT --mask 255.255.255.255 --rsource -j REJECT --reject-with tcp-reset
# Membuat Policy bahwa koneksi baru  harus dibawah 10 koneksi / 1 IP maximal 10 Koneksi jika lebih maka akan reject
-A INPUT -p tcp -m tcp --dport 21 -m conntrack --ctstate NEW -m connlimit --connlimit-above 10 --connlimit-mask 32 --connlimit-saddr -j REJECT --reject-with tcp-reset
# Jika memenu syarat maka koneksi di terima dan menuju status STABILIZE
-A INPUT -p tcp -m tcp --dport 21 -j ACCEPT
#******************** PING TRAFFIC ***********************************************
# Menset kedalam list address dan mengizinkan semua mask
-A INPUT -p icmp -m icmp --icmp-type 8 -m recent --set --name DEFAULT --mask 255.255.255.255 --rsource
# Update Policy Bahwa Hanya Bisa Di hit 3 kali dalam 1 detik jika Lebih maka akan reject
-A INPUT -p icmp -m icmp --icmp-type 8 -m recent --update --seconds 1 --hitcount 5 --name DEFAULT --mask 255.255.255.255 --rsource -j REJECT --reject-with icmp-host-unreachable
# Membuat Policy bahwa koneksi baru  harus dibawah 10 koneksi / 1 IP maximal 10 Koneksi jika lebih maka akan reject
-A INPUT -p icmp -m icmp --icmp-type 8 -m conntrack --ctstate NEW -m connlimit --connlimit-above 10 --connlimit-mask 32 --connlimit-saddr -j REJECT --reject-with icmp-host-unreachable
# Jika memenu syarat maka koneksi di terima dan menuju status STABILIZE
-A INPUT -p icmp -m icmp --icmp-type 8 -j ACCEPT
#********************* MIKROTIK TRAFFIC ******************************************
# Menset kedalam list address dan mengizinkan semua mask
-A INPUT -p tcp -m tcp --sport 8291 -m recent --set --name DEFAULT --mask 255.255.255.255 --rsource
# Update Policy Bahwa Hanya Bisa Di hit 3 kali dalam 1 detik jika Lebih maka akan reject
-A INPUT -p tcp -m tcp --sport 8291 -m recent --update --seconds 1 --hitcount 5 --name DEFAULT --mask 255.255.255.255 --rsource -j REJECT --reject-with tcp-reset
# Membuat Policy bahwa koneksi baru  harus dibawah 10 koneksi / 1 IP maximal 10 Koneksi jika lebih maka akan reject
-A INPUT -p tcp -m tcp --sport 8291 -m conntrack --ctstate NEW -m connlimit --connlimit-above 10 --connlimit-mask 32 --connlimit-saddr -j REJECT --reject-with tcp-reset
# Jika memenu syarat maka koneksi di terima dan menuju status STABILIZE
-A INPUT -p tcp -m tcp --sport 8291 -j ACCEPT
#********************** DNS TRAFFIC ***********************************************
# Membuat Policy bahwa koneksi baru  harus dibawah 100 koneksi / 1 IP maximal 100 Koneksi jika lebih maka akan reject
-A INPUT -p udp -m multiport --dports 53 -m conntrack --ctstate NEW -m connlimit --connlimit-above 100 --connlimit-mask 32 --connlimit-saddr -j REJECT --reject-with icmp-port-unreachable
# Jika memenu syarat maka koneksi di terima dan menuju status STABILIZE
-A INPUT -p udp -m multiport --dports 53 -j ACCEPT
# Membuat Policy bahwa koneksi baru  harus dibawah 100 koneksi / 1 IP maximal 100 Koneksi jika lebih maka akan reject
-A INPUT -p tcp -m multiport --dports 53 -m conntrack --ctstate NEW -m connlimit --connlimit-above 100 --connlimit-mask 32 --connlimit-saddr -j REJECT --reject-with tcp-reset
# Jika memenu syarat maka koneksi di terima dan menuju status STABILIZE
-A INPUT -p tcp -m multiport --dports 53 -j ACCEPT
#*********************** WEB TRAFFIC **********************************************
# Menset kedalam list address dan mengizinkan semua mask
-A INPUT -p tcp -m multiport --dports 80,443 -m recent --set --name DEFAULT --mask 255.255.255.255 --rsource
# Membuat Policy bahwa koneksi baru  harus dibawah 10 koneksi / 1 IP maximal 10 Koneksi jika lebih maka akan reject
-A INPUT -p tcp -m multiport --dports 80,443 -m recent --update --seconds 1 --hitcount 10 --name DEFAULT --mask 255.255.255.255 --rsource -j REJECT --reject-with tcp-reset
# Membuat Policy bahwa koneksi baru  harus dibawah 20 koneksi / 1 IP maximal 20 Koneksi jika lebih maka akan reject
-A INPUT -p tcp -m multiport --dports 80,443 -m conntrack --ctstate NEW -m connlimit --connlimit-above 20 --connlimit-mask 32 --connlimit-saddr -j REJECT --reject-with tcp-reset
# Jika memenu syarat maka koneksi di terima dan menuju status STABILIZE
-A INPUT -p tcp -m multiport --dports 80,443 -j ACCEPT
#*********************** DHCP TRAFFIC *********************************************
# Membuat Policy bahwa koneksi baru  harus dibawah 10 koneksi / 1 IP maximal 10 Koneksi jika lebih maka akan reject
-A INPUT -p udp -m multiport --dports 67,68 -m conntrack --ctstate NEW -m connlimit --connlimit-above 10 --connlimit-mask 32 --connlimit-saddr -j REJECT --reject-with icmp-port-unreachable
# Jika memenu syarat maka koneksi di terima dan menuju status STABILIZE
-A INPUT -p udp -m multiport --dports 67,68 -j ACCEPT
# Membuat Policy bahwa koneksi baru  harus dibawah 10 koneksi / 1 IP maximal 10 Koneksi jika lebih maka akan reject
-A INPUT -p udp -m multiport --sports 67,68 -m conntrack --ctstate NEW -m connlimit --connlimit-above 10 --connlimit-mask 32 --connlimit-saddr -j REJECT --reject-with icmp-port-unreachable
# Jika memenu syarat maka koneksi di terima dan menuju status STABILIZE
-A INPUT -p udp -m multiport --sports 67,68 -j ACCEPT
# ********************** NEIGHBOARD MIKROTIK TRAFFIC *****************************
# Membuat Policy bahwa koneksi baru  harus dibawah 10 koneksi / 1 IP maximal 10 Koneksi jika lebih maka akan reject
-A INPUT -p udp -m multiport --sports 5678 -m conntrack --ctstate NEW -m connlimit --connlimit-above 10 --connlimit-mask 32 --connlimit-saddr -j REJECT --reject-with icmp-port-unreachable
# Jika memenu syarat maka koneksi di terima dan menuju status STABILIZE
-A INPUT -p udp -m multiport --sports 5678 -j ACCEPT
# Membuat Policy bahwa koneksi baru  harus dibawah 10 koneksi / 1 IP maximal 10 Koneksi jika lebih maka akan reject
-A INPUT -p udp -m multiport --dports 5678 -m conntrack --ctstate NEW -m connlimit --connlimit-above 10 --connlimit-mask 32 --connlimit-saddr -j REJECT --reject-with icmp-port-unreachable
# Jika memenu syarat maka koneksi di terima dan menuju status STABILIZE
-A INPUT -p udp -m multiport --dports 5678 -j ACCEPT
# Membuat Policy bahwa koneksi baru  harus dibawah 10 koneksi / 1 IP maximal 10 Koneksi jika lebih maka akan reject
-A INPUT -p udp -m multiport --sports 5353 -m conntrack --ctstate NEW -m connlimit --connlimit-above 10 --connlimit-mask 32 --connlimit-saddr -j REJECT --reject-with icmp-port-unreachable
# Jika memenu syarat maka koneksi di terima dan menuju status STABILIZE
-A INPUT -p udp -m multiport --sports 5353 -j ACCEPT
#********************** OPENVPN TRAFFIC *****************************************
# Menset kedalam list address dan mengizinkan semua mask
-A INPUT -p tcp -m tcp --dport 1194 -m recent --set --name DEFAULT --mask 255.255.255.255 --rsource
# Update Policy Bahwa Hanya Bisa Di hit 3 kali dalam 1 detik jika Lebih maka akan reject
-A INPUT -p tcp -m tcp --dport 1194 -m recent --update --seconds 1 --hitcount 3 --name DEFAULT --mask 255.255.255.255 --rsource -j REJECT --reject-with icmp-port-unreachable
# Membuat Policy bahwa koneksi baru  harus dibawah 3 koneksi / 1 IP maximal 3 Koneksi jika lebih maka akan reject
-A INPUT -p tcp -m tcp --dport 1194 -m conntrack --ctstate NEW -m connlimit --connlimit-above 3 --connlimit-mask 32 --connlimit-saddr -j REJECT --reject-with icmp-port-unreachable
# Jika memenu syarat maka koneksi di terima dan menuju status STABILIZE dan jika gagal filter menjadi reject
-A INPUT -p tcp -m tcp --dport 1194 -j REJECT --reject-with icmp-admin-prohibited
#********************** L2TP ****************************************************
-A INPUT -p udp -m udp --sport 1701 -m recent --set --name DEFAULT --mask 255.255.255.255 --rsource
# Update Policy Bahwa Hanya Bisa Di hit 3 kali dalam 1 detik jika Lebih maka akan reject
-A INPUT -p udp -m udp --sport 1701 -m recent --update --seconds 1 --hitcount 3 --name DEFAULT --mask 255.255.255.255 --rsource -j REJECT --reject-with icmp-port-unreachable
# Membuat Policy bahwa koneksi baru  harus dibawah 3 koneksi / 1 IP maximal 3 Koneksi jika lebih maka akan reject
-A INPUT -p udp -m udp --sport 1701 -m conntrack --ctstate NEW -m connlimit --connlimit-above 3 --connlimit-mask 32 --connlimit-saddr -j REJECT --reject-with icmp-port-unreachable
# Jika memenu syarat maka koneksi di terima dan menuju status STABILIZE
-A INPUT -p udp -m udp --sport 1701 -j ACCEPT
#********************** IP Sec ****************************************************
# Membuat Policy bahwa koneksi baru  harus dibawah 10 koneksi / 1 IP maximal 10 Koneksi jika lebih maka akan reject
-A INPUT -p udp -m multiport --dports 500 -m conntrack --ctstate NEW -m connlimit --connlimit-above 10 --connlimit-mask 32 --connlimit-saddr -j REJECT --reject-with icmp-port-unreachable
# Jika memenu syarat maka koneksi di terima dan menuju status STABILIZE
-A INPUT -p udp -m multiport --dports 500 -j ACCEPT
#********************** IP Sec NAT ****************************************************
# Membuat Policy bahwa koneksi baru  harus dibawah 10 koneksi / 1 IP maximal 10 Koneksi jika lebih maka akan reject
-A INPUT -p udp -m multiport --dports 4500 -m conntrack --ctstate NEW -m connlimit --connlimit-above 10 --connlimit-mask 32 --connlimit-saddr -j REJECT --reject-with icmp-port-unreachable
# Jika memenu syarat maka koneksi di terima dan menuju status STABILIZE
-A INPUT -p udp -m multiport --dports 4500 -j ACCEPT
# Accept another connection
-A INPUT -p udp -j ACCEPT
# Show Log Rejected Connection
-A INPUT -j LOG --log-prefix "ip4-input-traffic-rejection: "
#&&&&&&&&&&&&&&&& FORWARD CONNECTION &&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&
-A FORWARD -j DOCKER-USER
-A FORWARD -j DOCKER-ISOLATION-STAGE-1
-A FORWARD -o docker0 -m conntrack --ctstate RELATED,ESTABLISHED -j ACCEPT
-A FORWARD -o docker0 -j DOCKER
-A FORWARD -i docker0 ! -o docker0 -j ACCEPT
-A FORWARD -i docker0 -o docker0 -j ACCEPT
-A FORWARD -i wlp* -o enp* -j ACCEPT
-A FORWARD -i ppp* -o ppp* -j ACCEPT
-A FORWARD -i wlp* -o enx* -j ACCEPT
-A FORWARD -i wlp* -o wlp* -m state --state RELATED,ESTABLISHED -j ACCEPT
#&&&&&&&&&&&&&&&& OUT CONNECTION &&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&
-A OUTPUT -o lo -j ACCEPT
# &&&&&&&&&&&&&&& DOCKER CONNECTION &&&&&&&&&&&&&&&&&&&&&&&&&&&&
-A DOCKER-ISOLATION-STAGE-1 -i docker0 ! -o docker0 -j DOCKER-ISOLATION-STAGE-2
-A DOCKER-ISOLATION-STAGE-1 -j RETURN
-A DOCKER-ISOLATION-STAGE-2 -o docker0 -j DROP
-A DOCKER-ISOLATION-STAGE-2 -j RETURN
-A DOCKER-USER -j RETURN

COMMIT
# Completed on Fri Jan 24 19:53:30 2025
# Generated by iptables-save v1.8.9 (nf_tables) on Fri Jan 24 19:53:30 2025
*nat
:PREROUTING ACCEPT [2572:497838]
:INPUT ACCEPT [2198:384654]
:OUTPUT ACCEPT [46538:6505588]
:POSTROUTING ACCEPT [46522:6504132]
:DOCKER - [0:0]
-A PREROUTING -m addrtype --dst-type LOCAL -j DOCKER
-A OUTPUT ! -d 127.0.0.0/8 -m addrtype --dst-type LOCAL -j DOCKER
-A POSTROUTING -s 172.17.0.0/16 ! -o docker0 -j MASQUERADE
-A DOCKER -i docker0 -j RETURN
COMMIT
# Completed on Fri Jan 24 19:53:30 2025
```